{
  "name": "Quiz",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "description": "Title of the personality quiz"
    },
    "description": {
      "type": "string",
      "description": "Optional description shown on title screen"
    },
    "title_image_url": {
      "type": "string",
      "description": "Optional background image for title screen"
    },
    "personalities": {
      "type": "array",
      "description": "List of personality outcomes",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string"
          },
          "description": {
            "type": "string"
          },
          "image_url": {
            "type": "string"
          },
          "result_url": {
            "type": "string",
            "description": "URL to link to from the result button for this personality"
          },
          "result_button_text": {
            "type": "string",
            "description": "Button text shown on the result screen for this personality"
          }
        }
      }
    },
    "questions": {
      "type": "array",
      "description": "List of quiz questions",
      "items": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string"
          },
          "image_url": {
            "type": "string"
          },
          "item_id": {
            "type": "string",
            "description": "Internal Item ID for admin use"
          },
          "facettes": {
            "type": "string",
            "description": "Facets for admin use"
          },
          "answers": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "text": {
                  "type": "string"
                },
                "personalities": {
                  "type": "string",
                  "description": "Comma-separated personality names"
                },
                "image_url": {
                  "type": "string"
                },
                "is_correct": {
                  "type": "boolean",
                  "description": "Indicates if this is the correct answer (internal, not shown to user)"
                }
              }
            }
          }
        }
      }
    },
    "button_color": {
      "type": "string",
      "default": "4D5DAA",
      "description": "Hex color for button accents"
    },
    "progress_color": {
      "type": "string",
      "default": "38B755",
      "description": "Hex color for progress bar"
    },
    "result_animation": {
      "type": "string",
      "enum": [
        "none",
        "fade-in",
        "wheel"
      ],
      "default": "fade-in"
    },
    "show_title_screen": {
      "type": "boolean",
      "default": true
    },
    "is_published": {
      "type": "boolean",
      "default": false
    }
  },
  "required": [
    "title"
  ]
}